[package]
name = "cyber_heist"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
godot = "0.5"
rand = "0.9"
serde = { version = "1", features = ["derive"] }
ron = "0.8"
